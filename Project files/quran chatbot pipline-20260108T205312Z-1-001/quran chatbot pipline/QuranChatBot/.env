# Example environment configuration for QuranChatBot

# OpenAI API key (required for embeddings and commentary)
OPENAI_API_KEY=

# Which OpenAI model to use for generation. Defaults to gpt-4o-mini. You can set this to
# gpt-3.5-turbo or another supported model if desired.
OPENAI_MODEL=gpt-4o-mini

# Which OpenAI embedding model to use. The current pipeline expects 1536‑dimensional
# vectors from `text-embedding-3-small`. If you change this model, update VECTOR_DIM
# accordingly.
OPENAI_EMBED_MODEL=text-embedding-3-small

# Timeout for OpenAI API calls in seconds.
OPENAI_TIMEOUT_S=60

# Qdrant connection settings. Adjust if your Qdrant instance runs elsewhere.
QDRANT_HOST=localhost
QDRANT_PORT=6333

# Qdrant collection names. Changing these will create new collections on next index.
QDRANT_COLLECTION_AYAHS=quran_ayahs_openai_v1
QDRANT_COLLECTION_WORDS=quran_words_openai_v1

# Vector dimension. Only change if you switch to an embedding model with a different output size.
VECTOR_DIM=1536

# Batch size for embedding requests (both indexing and search). Lower this if you encounter
# rate limits with the OpenAI API.
EMBED_BATCH_SIZE=64

# Default number of candidates to fetch during semantic search.
DEFAULT_TOP_K=7

# Optional path override for the ayah JSON file. If you deploy on a server where the
# working directory differs, set this to the absolute path of `metadata/ayahs_collection.json`.
# AYAHS_JSON_PATH=/path/to/ayahs_collection.json